# Planner Pay

Веб-сервис напоминаний об оплатах. Многопользовательский, с изолированными
кабинетами, дашбордом в стиле GitHub-активности, подсчётом расходов по месяцам
и автоматической рассылкой напоминаний по SMTP **за 14 дней** и **за 1 день**
до даты оплаты. Интерфейс в стиле macOS, светлая и тёмная темы.

## Возможности

- **Регистрация с подтверждением администратором** — новые заявки активируете вы
  (на дашборде админа показывается баннер с числом заявок).
- **Изолированные кабинеты** — каждый пользователь видит только свои оплаты.
- **Поля оплаты**: дата, сервис, сумма, описание (дату нельзя ставить задним числом).
- **Напоминания по почте** (SMTP) за 2 недели и за 1 день. У каждого пользователя
  можно указать **несколько** адресов — письмо уходит на все.
- **Ручная отправка** письма-сводки за выбранный период (7/14/30/90 дней или все).
- **Продление** — у оплаченной записи выбираете дату и сумму следующего платежа;
  старая помечается оплаченной (копится история).
- **Дашборд в стиле GitHub** — тепловая карта по дням (неделя Пн–Вс), при наведении
  раскрывается список оплат на дату; столбчатый график расходов по месяцам;
  суммы за год (всего / оплачено / предстоит).
- Светлая и тёмная темы.

## Стек

Python · FastAPI · SQLite (SQLAlchemy) · Jinja2 · APScheduler · Docker Compose.
Все секреты — в `.env`.

## Запуск локально (Docker)

```bash
cp .env.example .env        # заполните SECRET_KEY, ADMIN_*, SMTP_*
docker compose up --build
```

Откройте <http://localhost:8000>. Автоматически применяется
`docker-compose.override.yml`: приложение публикуется на порт 8000 напрямую,
а nginx и certbot не поднимаются (они в профиле `prod`).

Войдите администратором (`ADMIN_USERNAME` / `ADMIN_PASSWORD` из `.env`). Новые
пользователи регистрируются на `/register`, а вы подтверждаете их в разделе **Админ**.

База данных — обычный файл **`./data/planner_pay.db`**, его легко бэкапить.

> Без настроенного SMTP письма не отправляются, а пишутся в лог — удобно для
> разработки. Проверить отправку: **Настройки → Отправить тестовое письмо**.

## Запуск без Docker

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r app/requirements.txt
export $(grep -v '^#' .env | xargs)
export DATABASE_URL="sqlite:///./data/planner_pay.db"
uvicorn app.main:app --reload
```

## Деплой на сервере с доменом и SSL

В составе есть **nginx** (reverse-proxy, 80/443) и **certbot** (Let's Encrypt,
авто-продление). Домен — `planner-pay.it.ru`, почта для сертификата — `ppankin@it.ru`
(заданы в `nginx/conf.d/planner-pay.conf` и `init-letsencrypt.sh`).

1. Заведите **A-запись** `planner-pay.it.ru` → IP сервера. Откройте порты 80 и 443.
2. На сервере:
   ```bash
   git clone <repo> && cd planner-pay
   cp .env.example .env        # заполните SECRET_KEY, ADMIN_*, SMTP_*
   docker compose -f docker-compose.yml --profile prod build
   ./init-letsencrypt.sh       # разовый выпуск сертификата
   docker compose -f docker-compose.yml --profile prod up -d
   ```
   Флаг `-f docker-compose.yml` игнорирует локальный override, а `--profile prod`
   поднимает nginx и certbot.
3. Готово: <https://planner-pay.it.ru>

Приложение наружу напрямую не торчит (порт 8000 виден только внутри docker-сети) —
весь трафик идёт через nginx по HTTPS. Сертификат продлевается автоматически
(certbot раз в 12 ч, nginx перечитывает конфиг раз в 6 ч).

> Для отладки выпуска в `init-letsencrypt.sh` есть `staging=1` (тестовый сертификат
> Let's Encrypt без жёстких лимитов). Сменить домен/почту — там же и в конфиге nginx.

## Как работают напоминания

Планировщик (APScheduler) ежедневно в `REMINDER_HOUR:REMINDER_MINUTE`
(часовой пояс `TIMEZONE`) проверяет все неоплаченные записи и отправляет письмо,
если до даты осталось ровно 14 или 1 день. Флаги в БД исключают повторную отправку.

## Структура

```
app/
  main.py            точка входа, инициализация БД и админа, маршруты
  config.py          настройки из .env
  database.py        подключение к SQLite
  models.py          модели User и Payment
  security.py        пароли, сессии, проверки доступа
  email_utils.py     отправка писем по SMTP + шаблон письма
  reminders.py       логика рассылки и продления
  scheduler.py       ежедневная фоновая задача
  routers/           auth, dashboard, payments, admin, settings
  templates/         Jinja2-шаблоны (+ _icons.html — SVG-иконки)
  static/            CSS и JS (тепловая карта, тема, модалки)
nginx/conf.d/        конфиг reverse-proxy + HTTPS
certbot/             сертификаты Let's Encrypt (создаётся при выпуске)
init-letsencrypt.sh  разовый выпуск SSL-сертификата
docker-compose.yml   web + nginx + certbot
```

## Идеи для доработки

- Telegram-бот как альтернативный канал напоминаний.
- Категории/теги расходов и фильтры.
- Экспорт в CSV/Excel, графики динамики.
- Повтор оплаты с настраиваемым периодом (неделя/квартал/год), не только месяц.
- Мультивалютность и автоконвертация.
- Прикрепление чеков/файлов к оплате.
